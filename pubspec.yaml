name: instance_a
description: "A new Flutter project."

publish_to: "none"

version: 1.0.0+1

environment:
  sdk: ^3.8.1

dependencies:
  another_flushbar: ^1.12.30
  connectivity_plus: ^6.1.4
  cupertino_icons: ^1.0.8
  flutter:
    sdk: flutter
  flutter_bloc: ^9.1.1
  freezed_annotation: ^2.4.2
  get_it: ^8.0.3
  hive: ^2.2.3
  hive_flutter: ^1.1.0
  image_picker: ^1.1.2
  injectable: ^2.5.0
  logger: ^2.6.0
  path_provider: ^2.1.5
  permission_handler: ^12.0.0+1
  shimmer: ^3.0.0
  watcher: ^1.1.2

dev_dependencies:
  build_runner: ^2.4.6
  flutter_lints: ^5.0.0
  flutter_test:
    sdk: flutter
  freezed: ^2.4.5
  hive_generator: ^2.0.1
  injectable_generator: ^2.5.0
  json_serializable: ^6.7.1 # Needed for JSON serialization
  change_app_package_name: ^1.5.0
  # To change the package name of the app run the following command
  # dart run change_app_package_name:main com.new.package.name

flutter:
  uses-material-design: true
  assets:
    - assets/images/
